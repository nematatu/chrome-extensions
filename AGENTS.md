# Chrome Extensions 開発ルール

このファイルは、このリポジトリでAIが開発するときの共通ルールです。
対象の拡張機能ディレクトリに、さらに `AGENTS.md` がある場合は、その指示も適用します。

## 基本方針

- ユーザーの依頼を満たすために必要な範囲だけ変更する。
- 既存の挙動、設定、ファイル形式を確認してから編集する。
- 拡張機能ごとの独立性を保つ。依頼対象ではない拡張機能を変更しない。
- 不明な点は、現在のコード・README・テスト・実際のサイトの挙動を調べてから判断する。
- 推測だけで「対応済み」と報告しない。実行可能な検証で確認する。
- 破壊的な操作、外部サービスへの書き込み、公開、送信は依頼に明示されている場合だけ行う。

## 作業開始時

1. リポジトリの状態を確認する。
2. 対象ディレクトリの `manifest.json`、README、関連コード、テストを読む。
3. 未コミット変更を確認し、ユーザーの変更を上書きしない。
4. 依頼内容から、実装する要件と完了を証明する検証方法を整理する。

## 実装ルール

- Chrome Extension Manifest V3を前提にする。既存のmanifest設定を尊重する。
- 権限、host_permissions、外部通信先を追加するときは、必要性をREADMEに書く。
- ユーザー入力、URL、外部サイトのデータは信頼せず、許可対象・形式・サイズを検証する。
- ファイル名、保存先、HTML、URLを扱うときは、パス traversal、危険な文字列、意図しない外部URLを防ぐ。
- 外部サイトの仕様に依存する処理には、取得失敗、ログアウト、期限切れ、レート制限、仕様変更を考慮する。
- 個人情報、Cookie、アクセストークン、ダウンロードURLをログやテスト成果物に残さない。
- 既存の共有コードを変更するときは、利用箇所を検索して影響範囲を確認する。
- 大きなリファクタリング、依存パッケージの追加、仕様外のUI変更は、依頼に必要な場合だけ行う。

## テストと検証

- 変更後は、対象拡張機能に定義されたテストを実行する。
- テストがない場合は、変更のリスクに応じて構文確認、manifest確認、既存機能の動作確認を行う。
- UIやブラウザーAPIを変更した場合は、可能なら実ブラウザーで確認する。
- 外部サイトを使うテストでは、アクセスできなかったことを成功の証拠にしない。
- テストが失敗した場合、原因を直すか、未解決の理由を明示する。
- 最終報告では、変更内容、実行した検証、既知の制約を分けて書く。

## ドキュメント

- ユーザーが使う手順、保存先、権限、対応範囲、制約が変わったらREADMEを更新する。
- READMEには、Chromeへの読み込み方法と、必要なら更新後のタブ再読み込み方法を書く。
- 実装上の重要な前提は、チャットだけに残さずリポジトリ内に書く。

## Gitと引き継ぎ

- 変更前後に `git status` と差分を確認する。
- 無関係な変更をコミットに含めない。
- ユーザーが求めていないコミット、push、pull、merge、公開は行わない。
- 複数のMacで作業する場合、引き継ぎに必要な事実はコード、README、Issue、コミットメッセージのいずれかに残す。

## 完了条件

次のすべてを確認できたときだけ、依頼を完了したと報告する。

- 依頼された機能が現在のコードに存在する。
- 対象外の拡張機能や既存機能に不要な変更がない。
- 関連するテスト・検証が成功している。
- manifest、権限、READMEが実装と一致している。
- 既知の未対応ケースや外部サイト依存の制約を報告している。
